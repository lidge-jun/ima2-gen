# WP13 B — bounded release preparation

Implemented only the audited map. Main owns CHANGELOG/current procedure notes
and readiness records; two disjoint Astra/high workers supplied the CLI correction
and opt-in installed-artifact observation. Their reports live in ignored wp13/
b-cli-auth.md and b-published-ui.md; main read the actual diffs before integration.

- CLI now preserves real flat/nested provider401/403 identity using existing pure
  error owners. Reserved guard codes on either mixed side retain fixed safe text;
  malformed/unknown/aborted auth bodies are neutral SERVER_ACCESS_DENIED. Existing
  public statusForErrorCode is imported/re-exported after an unchanged pure move.
  Both standalone emitters include the required pure modules and errors directory.
  Worker RED10fail -> GREEN58/58; main independently reran58/58,0fail0skip.
- Existing package smoke gains an opt-in installed-server UI call. New manual-only
  workflow has explicit candidate/published acquisition, verified manifest/TGZ,
  distinct driver/product identity and two-size non-generating NovelAI capture.
  Existing source/publisher smokes have no browser work when the option is absent.
  The helper is193 lines, uses existing Playwright/pure fixture data, and is not
  shipped by the package allowlist. Main reviewed its native-vs-mocked boundary
  and syntax/dispatch-only read-permission configuration. Actual hosted capture
  remains unverified until it runs; no source screenshot stands in for publication.
- CHANGELOG adds bounded user-visible production-readiness notes under existing
  Unreleased, with HTTP/trusted-network and synthetic-verification limits. Current
  operations docs now accurately describe candidate CI before promotion, actual
  publisher/attestation jobs, preview consumer proof, manifest/SBOM attachments
  and the manual installed-artifact observation invocation.

Specific style tradeoff: the existing499-line CLI mock-owner file grows to589
with the required identity regression rows. Keep this one existing owner for
this release rather than extracting a new fixture API solely for the500-line
convention; no tests are deleted or compressed into opaque lines. Changed
production/helper files remain below500 and no test platform is introduced.

Read-only premerge snapshots verify18 PR heads against18 successful full-CI runs
and18 ancestry edges from approved dev prerequisite through217. Refresh again at
mutation time; these do not yet prove merges. New source changes invalidate b143
as release-prep acceptance. Native216 authority remains the explicit owner gate;
do not infer it from the generic merge request or create a heartbeat while waiting.
