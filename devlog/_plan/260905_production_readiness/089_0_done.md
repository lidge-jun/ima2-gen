# WP08c D — truthful local workflow observations

C verification is complete. Main closes the persisted work-phase after this
documentation checkpoint. Code freeze: `a1505d61b5fc28856e73d02b14b2d48852eed16d`.
PR210 is stacked on PR209 (`codex/prod-wp08-composer`). No merge or release yet.

## Delivered

The catalog client validates consumed fields instead of fabricating empty state.
One non-persisted resource shares initial/manual/focus reads, suppresses obsolete
responses, aborts on last unsubscribe and retains stale identity without stale
availability. Pure display projection separates lane readiness from the selected
image/video workflow. Current-state callbacks reject unavailable or stale choices.

Comfy stays a selectable setup destination, identified as Local HTTP. Its controls
show registered workflows and observation-based status, not GPT scalar controls.
Manager success invalidates the shared observation; failed writes do not. The
popup names the selected workflow and kind without a contradictory generic hint.
Provider identity remains visible, and error text is separated from the touch
target. No provider execution, persistence schema, auth or wire contract changed.

## Proof

- CI33993540499: all three enabled jobs succeeded. Node22 and Node24 each ran
  3149 tests:3145 pass,0fail,4existing skips. Package install/update/dry-run and
  graceful-shutdown checks passed. Native Chromium179/179 passed, including35
  new Comfy cases and all144 inherited cases (including77 composer components).
- CodeQL33993541780 succeeded. All93 alert identities and severities match WP08;
  no new alerts. Existing High/Critical debt remains a later release gate.
- Thirteen direct-file C tests:100PASS. UI/app/E2E/test typechecks and build passed;
  inventory, structure line counts, whitespace and blob budget passed. Existing
  chunk-size/dynamic-import build warnings remain unchanged.
- D1–D14 cover loading/error/retry, valid empty/missing/locked/disconnected state,
  per-workflow offline/lock versus available alternatives, deleted/opposite-kind
  identity, image/video same-ID and video-only setup, MCP priority, manager writes,
  concurrent subscriptions,401/403 and four locales at1280/390 plus320 recovery.
- Four source mutations genuinely failed then passed after exact restoration:
  obsolete response, stale-ready admission, offline row and stale callback context.
- Forty current PNGs (39 unique) and35 clean teardown records. Final A Kant and
  replacement B Mencius each directly viewed all40 paths. Main independently
  inspected six final key frames. Both explicit all-manifest verdicts are PASS;
  main expanded those compact verdicts over exact manifest paths/hashes, retaining
  raw reports and audited imageView traces. No inferred or old-capture PASS reuse.
- QA writer checked CI/source, manifest, hashes, actual view traces and both
  verdicts before the installed validator emitted an exit0 receipt.

Evidence root: `.codexclaw/evidence/01a06e88-aa93-77b2-a99a-fc10f8458eb2/wp08c/`.
Read `a1505d61-ci.json`, `a1505d61-codeql-evidence.json`,
`a1505d61-visual-manifest.json`, `a1505d61-oracle-{a,b2}.json`, their compact/raw
and view-provenance companions, and `qa-a1505d61/qa-receipt.json`.

## Repairs, failed assumptions and residuals

The first full CI exposed missing finite translation-call registration and a
duplicated brand literal. Both original i18n gates were repaired without weaker
assertions. Native green alone missed provider-label clipping, misleading popup
guidance, a below-fold capture and glued recovery text; direct image inspection
found these and fresh native captures verified the repairs. Earlier56 tests
silently ignored four wrong filename suffixes; corrected88 and then100-test
receipts are the credited evidence. All failed candidates are retained.

One reviewer claimed40 views but its actual trace had zero. Its PASS is rejected
and preserved as invalid evidence; a fresh independent reviewer actually opened
all40. The QA generator now requires direct-view provenance in addition to hashes.
Long repetitive report serialization was shortened, not the review requirement.

What did not improve: server liveness remains an observation, the offline suffix
is still a legacy DTO convention, hidden saved scalar settings retain their old
binding-dependent semantics, and this does not verify real generation. New stale
green state, a missing workflow identity or unexpected provider request would
falsify the current design. No paid/live provider or local-browser run occurred.

Next P is WP09, revalidating090 against this tree. Carry forward node HUD/default
fit, composition interruption, Home roster navigation,320px bottom-nav crowding
and the pre-existing actual-MCP readiness popup. WP12 keeps inherited security
alert/history-gate triage; WP12s and canonical release remain mandatory. Preserve
user scripts/recording, stashes, config/media and unrelated refs. The host goal
continues; this D is not a production-readiness or release completion claim.
