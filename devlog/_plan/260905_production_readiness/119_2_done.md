# WP11 — verified installation/runtime documentation contract

Outcome DONE for the installation work-phase, not the overall release goal.
Frozen verified source: aca964e0d184990a5b1d6d6b1cb2e3fc45152e27.
PR213 is stacked on212; no merge, release, npm publication or Pages deployment
is claimed. The closeout commit following this source changes this document only.

## Delivered

Three installers derive their minimum runtime from package metadata, attempt npm
once, retain caller/user processes, never perform global kill/cache/lock cleanup,
and require offline installation doctor before launch. Public copies/current
runtime docs are synchronized. Windows failures are catchable under irm|iex.
Windows candidate checkout/installer proof and release-bound Pages compatibility
are wired without modifying canonical release/publish workflows.

Built-runtime inventory is the actual existing npm pack manifest plus installed
JS execution, not the withdrawn standalone emitted-file drift checker. The
checker remains out of scope under112; no replacement framework was added.

## Fresh acceptance evidence

- FullCI34020135949 attempt1 SUCCESS ataca964; allfive jobs completed.
- Linux22.23.0/npm11.18.0 and24.17.0/npm12.0.0: each3246tests,3242pass,0fail,
  4existing omissions. Both packed/global-install/update/CLI lanes passed.
- Windows22.23.0/npm11.18.0 and24.17.0/npm12.0.0: each3250tests,3244pass,0fail,
  6existing omissions. Both packed runtime and CLI lanes passed. Both actual
  guard JSON records show expectedSha==actualSha==aca964.
- Each Windows lane executed9 unique installer scenarios in the dedicated step
  and again in the full suite; all unrelated sentinels stayed alive. Both POSIX
  scripts executed8 scenarios per Linux lane. No platform omission is passed off
  as actual shell execution. Personal Windows PowerShell policy was not changed.
- Each lane's actual pack inventory lists3633files, including403runtime/UI files;
  bin/ima2.js, server.js, config.js and ui/dist/index.html are present, app source
  TS is absent. Actual installed offline JSON/version/native/server checks passed.
- UI258passed(16.1m). Main opened currentaca964 sidebar1157x826, bottom1440x1000
  scrolled-to-Negative, and320x740 Korean long-input PNGs from artifact9985450214.
  Negative remains bounded, its scroll works and controls/Generate stay visible.
  Earlier a574 observations are supplementary; final captures are same-source.
- CodeQL34020136991 SUCCESS, analysis1731286007:93 unchanged alert-number/rule/
  severity tuples, added=[]/absent=[]. These are NOT resolved or waived; WP12 owns
  the mandatory triage/repair obligation. Filesystem34019935810 also SUCCESS.
- Local bounded receipt47pass/0fail/1hosted-only omission; both typechecks,
  runtime projection(changedPaths=[]), inventory and syntax/diff checks exit0.

Evidence root: .codexclaw/evidence/01a06e88-aa93-77b2-a99a-fc10f8458eb2/wp11/.
Read aca964-full-ci.json, aca964-*-evidence.json, aca964-codeql-evidence.json,
aca964-visual/{sidebar,bottom,mobile}.png,119-audit.md andaca964-review-synthesis.md.
The bound test receipt records the current source/epoch; this closeout is docs-only.

## Failures, dispositions and next work

Initial Windows failures were repaired with path-format/canonical-root correctness,
native child profile cleanup and fixture-directory warm-read before watching;
no path/account/network guard was weakened. The later Agent test EBUSY was an
owned polling worker reopening DB aftercloseDb. Controlled clock toggles proved
the mechanism; the existing stop function now runs before test DB cleanup, as
production shutdown already does. Native focused Windows15/15 and full22/24 pass.
This is not a general in-flight-worker quiescence guarantee; review caveat retained.

The old first-case MCP timeout remains historically unexplained. Diagnostic greens
were never called its fix. A distinct first-frame disconnect recovery gap was
reproduced by drop/deliver/drop; both obligations remain mandatory WP12/mcp-recovery
under audited119, before release. Port6665 rejection is separately recorded.
No new instrumentation campaign or broad guard platform is added to WP11.

The initial c-12 inventory wording gap was accepted and closed by exposing the
already-returned pack manifest, not by deleting the criterion. Main reclaimed
earlier incomplete worker deliveries and retained the audit/repair history.
Stale WP00 premise: E2E already has an explicit SHA guard in current ci.yml;
WP12 must revalidate that existing guard, not assume it is absent from110's plan.

Next: WP12 P revalidates120 against this landed tree, consumes current security/
MCP evidence and inherited history-gate obligation. Then WP12s andWP13 in order.
Verification15/18 WPs after this close; merged0; released0. Preserve user
scripts/recording/, credentials, media, branches and all existing work.
