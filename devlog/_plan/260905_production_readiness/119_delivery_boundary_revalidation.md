# WP11 P amendment — finish installation, retain lifecycle defects for WP12

Base b160e372cd45370ae4565bc31b0c0f884b7f1c2f. Class C4, repair archetype.
This revalidates110/111/112; it does not discard their implementation or restart
development. Prior C evidence is retained. FSM reset/re-entry records a scope
amendment, not a successful D. User's product-first/no-foundation-expansion
steering remains authoritative; no new permission or approval is fabricated.

Goal: complete the existing safe installation/runtime-doc/publication contract.
Non-goals: fixing all pre-existing CLI lifecycle defects inside the installer WP,
new test platforms, transport abstractions, guard matrices, dependency upgrades.
Stop: exact-current-head full CI (both Windows and Linux versions, packed smoke,
UI E2E), CodeQL delta inspection, installation behavior/negative cases, independent
review and source/docs parity; then WP11 D and proceed to WP12. No gate skip or
green-on-retry remediation claim. Existing four-hour reassessment/72-hour goal
bound, zero paid calls and scoped GitHub/owned-fixture authorities remain.

## Classified current delta

Product: installers, metadata projection/current docs, offline doctor integration,
release-bound Pages contract (110/111). All implemented; no new product patch
planned in this amendment.
Necessary verification: original installer scenarios9 on Windows and16 on POSIX,
projection drift/invalid inputs, Pages guards, explicit current SHA, existing
packed runtime and full final-candidate CI. Existing48-case local contract receipt
has47pass/0fail/1hosted-only omission; that is NOT hosted installation proof.
Auxiliary diagnostics: temporary Windows job and118's failure-only traces/rate
measurements. No further instrumentation framework or repeated rate campaign in
WP11. Existing sources/tests/assertions remain present in the full suite.

## Exact boundary change, not a release waiver

The original cli-model-resolver first-case timeout has no established historical
cause. b160 root-suite diagnostics passed twice (3244/3250,0fail,6existing skips),
and the prior focused30-process measurement passed. These are observations, not a
fix. Controlled native SSH drop/deliver/drop exposed a separate reproducible
first-frame SSE recovery gap. Both belong to WP12 integrated lifecycle acceptance;
neither is an installation contract defect proven by the available evidence.

Add a mandatory WP12 task to preserve BOTH obligations: diagnose/disposition the
original failure with actual evidence and repair/verify first-frame disconnect
recovery without changing pending/error/deadline semantics. Also record the
personal-host port6665 fixture rejection as a distinct follow-up, not the20s
incident. No skip, allowlist, timeout increase or permission weakening is allowed.
WP12 cannot close merely because another run is green. These tasks remain open
before ordered merge/release; the global goal and final release criteria do not
shrink. Existing93 CodeQL alerts likewise remain mandatory WP12 work.

This supersedes the main's unnecessary approval-wait statement in the ignored
b160-root-ci-observation.md. The owner already requested product-first scope
control, preserved final verification and sequential WPs. This amendment changes
work allocation only; it does NOT authorize release with unresolved findings.

## Diff and verifier map

- NEW119_delivery_boundary_revalidation.md: this scope/evidence decision.
- Existing goalplan: add WP12/mcp-recovery task using cxc loop add-task; leave all
  original tasks/criteria/statuses intact, no manual completion edits.
- Existing110/111/112 code manifest: NO additional implementation delta. Prior
  commits34e67201 throughb160e372 and113/114 review syntheses are inherited.
- Existing118 and evidence/wp11: retain historical failures, sample limitations,
  controlled disconnect, and same-tree visual observations; do not rewrite history.

Executed current-head verifiers: both typechecks exit0; docs:runtime:check exit0
with changedPaths=[]; test:inventory exit0; explicit48-case command in the bound
test receipt47pass/0fail/1hosted-only omission. The explicit test paths observe
installers/projection/Pages/release owners. No local app/account/server was run.
Full CI and CodeQL must still run at the amended candidate's frozen full SHA;
prior a574 or b160 diagnostic-only proof cannot substitute.

Main owns all transitions, commits and workflow dispatch. A reviewer reads this
amendment plus110/111/112 and the actual code/evidence, with no code changes.
Model and effort omitted. No speculative WP12 implementation during WP11 B/C.
After A, B checkpoints this documentation/allocation-only amendment; C performs
the missing exact-head gates. SoT remains current README/AGENTS/structure docs
already updated by WP11; no new SoT tooling. Rollback is the existing110/111
contract; never restore global process-kill/cache-clean behavior as a workaround.
