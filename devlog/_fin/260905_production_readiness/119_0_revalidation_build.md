# WP11 B amendment checkpoint

Independent A reviewer Darwin first found c-12 inventory mapping absent (High).
Main accepted the evidence/documentation gap, preserved the criterion, and added
the concrete119 fold-back atf43fe67a. Same reviewer re-audited: VERDICT PASS.
The withdrawn standalone source/emitted drift checker is not reintroduced.

tests/package-install-smoke.mjs now prints name/version/files from its EXISTING
fresh npm pack manifest. Two executable lines; no extra pack/install/file traversal,
new helper, dependency, guard, assertion, timeout, skip or permission change.
The existing provided-tarball branch is unchanged and does not claim this fresh
manifest log. Main searched parsePackOutput and inspected its original-object
return plus the whole consumer; package files/prepack remain authoritative.

Prior installer/projection/Pages product commits are inherited unchanged. Source
syntax is checked without running a local installation/server/account probe.
C must read the actual full-SHA hosted inventory and installed-runtime outputs;
this source edit is not acceptance proof. The existing full matrix and CodeQL
remain mandatory. WP12/mcp-recovery retains unresolved lifecycle obligations and
all other global release gates remain open until their planned phases finish.

Rollback of this amendment removes only the log statement and restores the prior
allocation documentation; never suppress existing installation/CI assertions.
