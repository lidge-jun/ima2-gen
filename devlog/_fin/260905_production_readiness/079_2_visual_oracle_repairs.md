# WP07 C — visual oracle synthesis and bounded repair

Kuhn/Galileo each directly viewed84novelPNG and distinguished23prior exact matches
and25current duplicates atcefd6d3b. Both REVISE; files visual-oracle-a/b.json retain
full coverage and findings. Main independently viewed the node,320px Korean and
extension captures. Allautomated53E2E and3125root rows perruntime were green;
those successes do not override observed semantics or occluded capture gaps.

## A01/B01: accept node hint correction

Root: NodeErrorInfo's generic no-CTA fallback is fix-input; ImageNode renders its
generic prompt/reference regeneration hint even for completion-unknown tracking.
MODIFY ui/src/components/ImageNode.tsx: derive errorAction=null for exact
d.errorInfo.code JOB_TRACKING_TIMEOUT, otherwise keep existing status/action logic.
No new persisted field/action enum, no message-string recognition, no change to
nontracking actions. Existing full status title and global warning remain.
MODIFY J7b: assert no .image-node__error-cta and exact full warning title, in
addition to noRetry,ordinaryGen/onePOST and actual terminal toast.

## A02/B02: accept after-dismiss inline proof

390px captures contain the correct toast but it overlays the persistent notice;
viewport rectangles alone missed paint/hit occlusion. Keep initial live-toast
PNG and fulltext/noCTA assertions. Then use the real toast Dismiss button and
assert toast0 while inline remains. For each existingfiveviewport, normally scroll
the inline notice toward viewport center, then capture literal DOM text, text-line
center hit tests, inlineDismiss hit tests and fixed-mobile-nav intersection.
Center scrolling is normal surface driving, not CSS hiding or store injection.
No extra generationPOST. If native metrics still show occlusion, retainFAIL and
inspect before changing product layout; do not merely relax the test.

## A03/B03: accept unobscured Retry capture

The ordinary extension Retry is enabled and later clicked successfully, but its
PNG is covered by the retained tracking toast. Retain initial outcome frames;
normally dismiss toasts between outcomes, assert empty toast stack, then capture
the same disabled tracking control/ordinary Retry/source-change states unobscured.
Add actual center hit-test for ordinary Retry before its already-planned third
POST. Preserve exact3POST, same-source disabled/noRetry and pending-source behavior.

## Sequencing / boundaries

Main writes ImageNode+J7b only, plus this record. No source changed until both
oracle passes completed. They will re-review closure using new-head captures and
may reuse only exact bytes they actually approved; cefdREVISE remains preserved.
Initial canvas MiniMap/default-fit and selected top-toolbar overlap are recorded
baseline HUD observations, not fixed by this unit. Actual zoom activation is proven;
no global canvas layout claim is made. Future node-journey P revalidation must keep
these observations, not misrepresent them as new cancellation regressions.

Fresh native CIscreenshots/geometry, types/builds/tests and coherent receipts are
required after this patch. Do not close on source-only assertions or old imagery.
